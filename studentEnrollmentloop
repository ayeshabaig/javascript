var Students = [/*..*/];

for (let i = 0; i < students.length; i++) {
      greetStudent( students[i] );
  
  for  (let students of students) {
        greetStudent( student)
  }
 while (students.length . 0) {
    let student = students.pop();
    greetStudent(student);
  }
functon greetStudent(student) {
    console.log(
    'Hello, ${student.name}'
    )
  }
